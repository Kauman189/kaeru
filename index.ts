import "react-native-gesture-handler";
import { registerRootComponent } from "expo";

import App from "./App";

// registerRootComponent registra el componente principal para Expo Go o builds nativos.
registerRootComponent(App);
